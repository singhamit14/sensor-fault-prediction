pymongo==4.2.0
requests==2.32.3

-e .  # editable mode (we do not require every time ti install setup.py whenever we run requirement.txt when he find -e . it automatically add these packages in setp file)
